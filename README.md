# filtering_task_resolute

## NPM Install

Run `npm i` to install node modules the project.

## JSON Server

Run `json-server db.json` to build the dummy datadirectory.

## Start App

Run `ng serve` to run the app.